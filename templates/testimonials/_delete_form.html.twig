<form method="post" action="{{ path('testimonials_delete', {'id': testimonial.id}) }}" onsubmit="return confirm('Êtes-vous sûr(e) de vouloir supprimer votre témoignage ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ testimonial.id) }}">
    <button class="custom-btn">Supprimer</button>
</form>
