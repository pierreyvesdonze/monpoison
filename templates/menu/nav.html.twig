<nav class="navbar navbar-expand-lg navbar-light">
    <div class="container-fluid">

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            |||
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('home') }}">Accueil</a>
                </li>
                {% if app.user %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        Mon profil
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="{{ path('user_account') }}" title="Tableau de bord de mes consommations">Tableau de bord</a></li>
                        <li><a class="dropdown-item" href="{{ path('drink_calendar') }}" title="Suivi de mes consommations">Mes consommations</a></li>
                        <li>
                            <a class="dropdown-item" aria-current="page" href="{{ path('logout') }}" title="Déconnexion">Se
                                déconnecter</a>
                        </li>
                    </ul>
                </li>

                {% else %}

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        Mon profil
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li>
                            <a href="{{path('login')}}" type="button" class="dropdown-item" title="Se connecter">Se connecter</a>
                        </li>
                        <li>
                            <a href="{{path('app_register')}}" type="button" class="dropdown-item" title="S'enregistrer">S'enregistrer</a>
                        </li>
                    </ul>
                </li>
                {% endif %}

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('alcool_test') }}" title="Test de dépendance">Test de dépendance</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('alcool_stats') }}" title="Quelques chiffres sur l'alcool">L'alcool en chiffres</a>
                </li>
                <li class="nav-item">

                </li>
            </ul>
        </div>
    </div>
</nav>